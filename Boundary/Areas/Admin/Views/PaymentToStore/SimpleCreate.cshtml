@model DataModel.Entities.RelatedToPayments.HBPaymentToStore

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
</head>
<body>
    @using (Html.BeginForm("PostCreate", "PaymentToStore", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.OrderCode)
        
        <div class="form-horizontal">
            <h4>HBPaymentToStore</h4>
            <hr />
            @Html.ValidationSummary(true)
    
            <div class="form-group">
                @Html.LabelFor(model => model.TrackingCode, new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.TrackingCode)
                    @Html.ValidationMessageFor(model => model.TrackingCode)
                </div>
            </div>
    
            <div class="form-group">
                @Html.LabelFor(model => model.Money, new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Money)
                    @Html.ValidationMessageFor(model => model.Money)
                </div>
            </div>
    
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Save" class="btn btn-default" />
                </div>
            </div>
        </div>
    }
    
    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</body>
</html>
